# autosuggest

verifying the git set-up : )

changes flow for pushing
UI
1. add(plus button)
2. commit with message
3. push / sync

commands to add commit in terminal:
git  add -A
git commit -m "some message"
git push